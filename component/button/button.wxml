<view class='main {{melody}} custom-class' style='{{}}' bindtap='handleTap'>
  <view wx:if='{{loading.show}}' class='loading' style='width:{{loading.percent}}'></view>
  <view class='text'>
    <slot></slot>
  </view>
  <view class='cover' hover-class='cover-hover'></view>
</view>